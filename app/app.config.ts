export default defineAppConfig({
  ui: {
    colors: {
      primary:   'ispblue',
      secondary: 'isporange',
      success:   'green',
      info:      'sky',
      warning:   'amber',
      error:     'red',
      neutral:   'slate',
    },
  },
});
